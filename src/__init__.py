"""gemini-nano-banana-discord-bot package metadata.

This module exposes the package version for build tooling that imports
the package to read metadata (some setuptools helpers expect __version__).
"""

__version__ = "0.1.0"
